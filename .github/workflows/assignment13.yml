name: Assignment 13

on:
push:
pull_request:

jobs:
test:
runs-on: ubuntu-latest

```
steps:
  - name: Checkout code
    uses: actions/checkout@v4

  - name: Start MySQL
    run: |
      sudo systemctl start mysql
      mysql -u root -e "CREATE DATABASE IF NOT EXISTS CollegeDB;"

  - name: Run Assignment Test
    run: |
      chmod +x Assignment_13/test.sh
      ./Assignment_13/test.sh
```
